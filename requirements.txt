# Core framework (API layer)
fastapi==0.115.0
uvicorn==0.30.6

# Data validation
pydantic==2.8.2

# Database (optional but common in PGMS)
sqlalchemy==2.0.32

# Environment variables management
python-dotenv==1.0.1

# Dependency injection (useful in clean architecture)
dependency-injector==4.41.0

# Testing
pytest==8.3.2
pytest-asyncio==0.23.8

# HTTP requests (if needed for external services)
httpx==0.27.0

# Logging improvement (optional but useful)
loguru==0.7.2